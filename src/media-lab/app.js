/* navigator.mediaDevices.enumerateDevices().then(function(devices) {
    console.log(devices);
}
).catch(function(err) {
    console.log(err.name + ": " + err.message);
}
); */

// todo work with node usb communication to deal with camera buffers

console.log('todo work with node usb communication to deal with camera buffers')